const authJwt = require("./authJwt");
const verifySolution = require("./verifySolution");
const verifyScreen = require("./verifyScreen");
const verifyWidget = require("./verifyWidget");
module.exports = {
  authJwt,
  verifySolution,
  verifyScreen,
  verifyWidget
}